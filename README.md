# kotoba-lang/level

**SSoT for `kami.level`** — spawns, storm zone, objectives as pure EDN (`.cljc`).

`kotoba.level` is a thin facade. ADR-2607102200 addendum 8.

## Test

```sh
clojure -M:test
```
